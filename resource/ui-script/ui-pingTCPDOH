#!/bin/bash
url=$(jq -r --arg doh "doh$1" '.dns."\($doh)"' /opt/de_GWD/0conf | cut -d/ -f1)
echo "$(curl -o /dev/null -sw '%{time_connect}\n' https://$url) * 1000" | bc | awk '{printf ("%.0f\n",$1)}' 
